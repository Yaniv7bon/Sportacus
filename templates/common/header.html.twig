<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
  <a class="navbar-brand" href="{{path("home")}}">SPORTACUS</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarColor01">
  <ul class="navbar-nav mr-auto">
    <li class="nav-item active">
        <a class="nav-link" href="{{path('zone_publique')}}">Zone publique <span class="sr-only">(current)</span></a>
    </li>
    <li class="nav-item active">
        <a class="nav-link" href="{{path('blog_home')}}">Le Blog <span class="sr-only">(current)</span></a>
    </li>
  </ul> 
    <ul class="navbar-nav ml-auto">
    {% if app.user %}
    {% if is_granted('ROLE_ADMIN') %}
      <li class="nav-item">
        <a href="{{path("admin_users")}}" class="nav-link">
          <i class="fas fa-eye"></i>
            Backoffice
          
        </a>
      </li>
    {% endif %}
      <li class="nav-item dropdown">
        <a href="#" class="nav-link dropdown-toggle" id="accountDropdownLink" data-toggle="dropdown">
        <img src="{{app.user.picture}}" class="avatar avatar-mini" alt="">
						{{app.user.firstname}}
				</a>
        <div class="dropdown-menu dropdown-menu-right" aria-labelledby="accountDropdownLink">
						{% if app.user.usertraining %}
						<a href="{{path('training_auto')}}" class="dropdown-item">Mon programme</a>
            {% else %}
						<a href="{{path('account_index')}}" class="dropdown-item">Mon programme</a>
            {% endif %}
          	<a href="{{path("account_index")}}" class="dropdown-item">Mon programme sur-mesure</a>
            <a class="dropdown-item" href="{{path("account_logout")}}">Deconnexion</a>
      	</div>
      </li>
      {% else %}
      <li class="nav-item">
        <a class="nav-link" href="{{path("account_login")}}">Connexion</a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="{{path("account_register")}}">Inscription</a>
      </li>
       {% endif %}
    </ul>
    {#<form class="form-inline my-2 my-lg-0">
      <input class="form-control mr-sm-2" type="text" placeholder="Search">
      <button class="btn btn-secondary my-2 my-sm-0" type="submit">Search</button>
    </form>
    <li class="nav-item">
        <a class="nav-link" href="{{path("account_index")}}">Mon profil</a>
      </li>#}
  </div>
</nav>

{% for label, messages in app.flashes %}

	<div class="container">
		<div class="alert alert-{{label}}">
			{% for message in messages %}
				<p>{{message|raw}}</p>
			{% endfor %}
		</div>
	</div>

{% endfor %}
