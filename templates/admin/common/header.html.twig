<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
  <a class="navbar-brand" href="{{path("home")}}">SPORTACUS</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarColor01">
  <ul class="navbar-nav mr-auto">
    <li class="nav-item active">
        <a class="nav-link" href="{{path('zone_publique')}}">Backoffice <span class="sr-only">(current)</span></a>
    </li>
    <li class="nav-item">
        <a href="{{path("admin_users")}}" class="nav-link">
          <i class="fas fa-users fa-lg"></i>
            
        </a>
      </li>
    <li class="nav-item">
        <a href="{{path("admin_exos")}}" class="nav-link">
          <i class="fas fa-dumbbell fa-lg"></i>
            
        </a>
      </li>
  </ul> 
    <ul class="navbar-nav ml-auto">
  

      <li class="nav-item">
        <a href="{{path("home")}}" class="nav-link">
          <i class="fas fa-home fa-lg"></i>
            Accueil
        </a>
      </li>

      <li class="nav-item dropdown">
        <a href="#" class="nav-link dropdown-toggle" id="accountDropdownLink" data-toggle="dropdown">
        <img src="{{app.user.picture}}" class="avatar avatar-mini" alt="">
						{{app.user.firstname}}
				</a>
        <div class="dropdown-menu dropdown-menu-right" aria-labelledby="accountDropdownLink">
            <a class="dropdown-item" href="{{path("account_logout")}}">Deconnexion</a>
      	</div>
      </li>
    </ul>
  </div>
</nav>

{% for label, messages in app.flashes %}

	<div class="container">
		<div class="alert alert-{{label}}">
			{% for message in messages %}
				<p>{{message|raw}}</p>
			{% endfor %}
		</div>
	</div>

{% endfor %}
